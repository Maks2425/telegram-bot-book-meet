# Telegram Bot - Book Meet

Telegram-бот для клінінгової компанії на Python з використанням aiogram 3.x. Бот дозволяє клієнтам розраховувати вартість прибирання та бронювати послуги з інтеграцією в Google Calendar.

## Функціонал

- 📊 **Розрахунок вартості прибирання** - автоматичний розрахунок з урахуванням типу прибирання, типу житла та площі
- 📅 **Бронювання послуг** - вибір дати та часу з перевіркою доступності в Google Calendar
- 📍 **Поділитися локацією** - можливість поділитися координатами або ввести адресу вручну
- 🔔 **Сповіщення власника** - автоматичні повідомлення про нові бронювання
- 🗓️ **Інтеграція з Google Calendar** - автоматичне створення подій у календарі
- 💰 **Система знижок** - автоматичні знижки для великих площ

## Встановлення та налаштування

### 1. Створення віртуального середовища

```powershell
python -m venv venv
```

### 2. Активація віртуального середовища

**Windows (PowerShell):**
```powershell
.\venv\Scripts\Activate.ps1
```

**Windows (Command Prompt):**
```cmd
venv\Scripts\activate.bat
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

### 3. Встановлення залежностей

Після активації venv:
```powershell
pip install -r requirements.txt
```

Або безпосередньо через Python з venv:
```powershell
.\venv\Scripts\python.exe -m pip install -r requirements.txt
```

### 4. Налаштування Telegram бота

1. Створіть бота через [@BotFather](https://t.me/BotFather) в Telegram
2. Отримайте токен бота
3. Скопіюйте файл `.env.example` в `.env`:
   ```powershell
   Copy-Item .env.example .env
   ```

4. Відкрийте файл `.env` і додайте токен:
   ```env
   BOT_TOKEN=ваш_токен_від_BotFather
   ```

### 5. Налаштування Google Calendar (опціонально, але рекомендовано)

Для повноцінної роботи бронювання необхідно налаштувати Google Calendar API.

**Швидкий старт:**
1. Перегляньте детальну інструкцію в файлі [`GOOGLE_CALENDAR_SETUP.md`](GOOGLE_CALENDAR_SETUP.md)
2. Створіть Service Account та завантажте JSON ключ
3. Додайте шлях до ключа в `.env`:
   ```env
   GOOGLE_SERVICE_ACCOUNT_FILE=service-account-key.json
   GOOGLE_CALENDAR_ID=primary
   ```

**Важливо:** Без налаштованого Google Calendar бот все одно працюватиме, але:
- Не буде перевірки доступності слотів
- Події не будуть створюватися в календарі
- Будуть показуватися всі можливі дні та часи

### 6. Налаштування сповіщень власника (опціонально)

Додайте свій Telegram ID для отримання сповіщень про нові бронювання:
```env
OWNER_TELEGRAM_ID=123456789
```

**Як знайти свій Telegram ID:**
- Напишіть боту [@userinfobot](https://t.me/userinfobot) в Telegram
- Він надішле вам ваш ID
- Скопіюйте число та додайте в `.env` файл

**Важливо:** Перед отриманням сповіщень власник має написати боту `/start`.

## Запуск бота

### Варіант 1: З активацією venv

1. Активуйте віртуальне середовище:
   ```powershell
   .\venv\Scripts\Activate.ps1
   ```

2. Запустіть бота:
   ```powershell
   python main.py
   ```

### Варіант 2: Безпосередньо через Python з venv

```powershell
.\venv\Scripts\python.exe main.py
```

## Деактивація віртуального середовища

Коли закінчите роботу:
```powershell
deactivate
```

## Структура проекту

```
telegram-bot-book-meet/
├── .env                      # Файл з конфігурацією (не комітиться в git)
├── .env.example              # Приклад конфігурації
├── .gitignore                # Ігнорування файлів для git
├── config.py                 # Модуль конфігурації
├── main.py                   # Головний файл бота
├── states.py                 # FSM стани для процесу бронювання
├── requirements.txt          # Залежності проекту
├── GOOGLE_CALENDAR_SETUP.md  # Інструкція з налаштування Google Calendar
│
├── handlers/                 # Обробники подій
│   ├── __init__.py
│   ├── start.py              # Обробка команди /start
│   ├── callbacks.py          # Обробка натискань кнопок
│   └── cleaning.py           # Обробка FSM станів (розрахунок, бронювання)
│
├── keyboards/                # Клавіатури
│   ├── __init__.py
│   ├── start.py              # Стартова клавіатура
│   └── cleaning.py           # Клавіатури для процесу бронювання
│
└── services/                 # Бізнес-логіка
    ├── __init__.py
    ├── pricing.py            # Розрахунок вартості прибирання
    ├── calendar_service.py   # Робота з Google Calendar API
    └── date_utils.py         # Утиліти для роботи з датами
```

## Команди бота

- `/start` - Привітання та показ головного меню

## Процес бронювання

1. **Розрахунок вартості:**
   - Вибір типу прибирання (Підтримуюче, Генеральне, Після ремонту)
   - Вибір типу житла (Квартира, Будинок)
   - Введення площі в м²
   - Автоматичний розрахунок вартості з урахуванням знижок

2. **Бронювання:**
   - Вибір дати (показуються тільки дні з доступними слотами)
   - Вибір часу (показуються тільки вільні часові слоти)
   - Введення адреси або поділитися локацією
   - Автоматичне створення події в Google Calendar
   - Сповіщення власника про нове бронювання

## Налаштування робочого часу

Робочий час налаштовується в `config.py`:

```python
CALENDAR_TIMEZONE = "Europe/Stockholm"  # Часовий пояс
CALENDAR_WORK_START = time(8, 0)        # Початок робочого дня (8:00)
CALENDAR_WORK_END = time(18, 0)         # Кінець робочого дня (18:00)
CALENDAR_SLOT_INTERVAL_HOURS = 2        # Інтервал між слотами (2 години)
CALENDAR_CLEANING_DURATION_HOURS = 2     # Тривалість прибирання (2 години)
```

## Безпека

⚠️ **Важливо:** 
- Ніколи не комітьте файл `.env` в git
- Файл `.env` вже додано в `.gitignore`
- Не комітьте файли з ключами Google Calendar (`*-key.json`, `service-account-key.json`)
- Не діліться своїм токеном бота з іншими
- Зберігайте Service Account ключі в безпечному місці

## Вимоги

- Python 3.8+
- pip
- Google Cloud Platform акаунт (для Google Calendar API)
- Telegram бот токен від [@BotFather](https://t.me/BotFather)

## Залежності

Проект використовує:
- `aiogram==3.13.1` - сучасна бібліотека для роботи з Telegram Bot API
- `python-dotenv==1.0.0` - завантаження змінних оточення з `.env` файлу
- `google-api-python-client==2.108.0` - клієнт для Google Calendar API
- `google-auth-httplib2==0.1.1` - аутентифікація для Google API
- `google-auth-oauthlib==1.2.0` - OAuth2 для Google API
- `tzdata==2024.1` - дані про часові пояси

## Розробка

### Особливості архітектури

- **Модульна структура** - код розділений на handlers, keyboards, services
- **FSM (Finite State Machine)** - для керування станами процесу бронювання
- **Асинхронна обробка** - використання async/await для ефективної роботи
- **Інтеграція з Google Calendar** - автоматична перевірка доступності та створення подій
- **Система знижок** - автоматичні знижки залежно від площі (до 15%)

### Додаткова документація

- [`GOOGLE_CALENDAR_SETUP.md`](GOOGLE_CALENDAR_SETUP.md) - детальна інструкція з налаштування Google Calendar API

## Підтримка

Якщо виникли проблеми:
1. Перевірте, чи правильно налаштований `.env` файл
2. Переконайтеся, що всі залежності встановлені
3. Перевірте логи бота на наявність помилок
4. Переконайтеся, що Google Calendar API налаштований правильно (якщо використовується)
