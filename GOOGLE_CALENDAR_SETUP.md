# Налаштування Google Calendar API (Service Account)

Детальна інструкція для налаштування Google Calendar API з використанням Service Account (рекомендований метод для ботів).

## Крок 1: Створення проекту в Google Cloud Console

1. Перейдіть на [Google Cloud Console](https://console.cloud.google.com/)
2. Увійдіть у свій Google акаунт
3. Натисніть на випадаюче меню проекту (зверху) і виберіть **"New Project"** (Новий проект)
4. Введіть назву проекту (наприклад: "Telegram Bot Calendar")
5. Натисніть **"Create"** (Створити)
6. Дочекайтеся створення проекту (зазвичай кілька секунд)

## Крок 2: Увімкнення Google Calendar API

1. У Google Cloud Console перейдіть до **"APIs & Services"** → **"Library"** (Бібліотека)
2. У пошуку введіть **"Google Calendar API"**
3. Виберіть **"Google Calendar API"** зі списку
4. Натисніть кнопку **"Enable"** (Увімкнути)
5. Дочекайтеся активації API (зазвичай кілька секунд)

## Крок 3: Створення Service Account

1. Перейдіть до **"APIs & Services"** → **"Credentials"** (Облікові дані)
2. Натисніть **"+ CREATE CREDENTIALS"** (Створити облікові дані)
3. Виберіть **"Service Account"** зі списку
4. Заповніть форму:
   - **Service account name**: `telegram-bot-calendar` (або будь-яка назва)
   - **Service account ID**: автоматично заповниться (можна залишити як є)
   - **Description**: `Service account for Telegram bot calendar integration` (опціонально)
5. Натисніть **"Create and Continue"** (Створити і продовжити)
6. У розділі **"Grant this service account access to project"** (Надати доступ до проекту):
   - **Role**: виберіть **"Editor"** або **"Owner"** (для повного доступу)
   - Або залиште порожнім (можна налаштувати пізніше)
7. Натисніть **"Continue"** (Продовжити)
8. У розділі **"Grant users access to this service account"** (Надати доступ користувачам):
   - Можна пропустити (натисніть **"Done"**)

## Крок 4: Створення та завантаження ключа

1. У списку Service Accounts знайдіть створений акаунт (наприклад, `telegram-bot-calendar`)
2. Натисніть на нього
3. Перейдіть на вкладку **"KEYS"** (Ключі)
4. Натисніть **"ADD KEY"** → **"Create new key"** (Створити новий ключ)
5. Виберіть формат **"JSON"**
6. Натисніть **"Create"** (Створити)
7. Файл автоматично завантажиться на ваш комп'ютер (назва типу: `project-name-xxxxx.json`)

⚠️ **ВАЖЛИВО**: Збережіть цей файл у безпечному місці! Він містить секретні дані для доступу до вашого календаря.

## Крок 5: Налаштування календаря для Service Account

Service Account має власну електронну адресу (виглядає як `telegram-bot-calendar@project-name.iam.gserviceaccount.com`).

### Варіант A: Використання основного календаря (рекомендовано)

1. Відкрийте свій основний Google Calendar
2. У лівому меню знайдіть **"Settings"** (Налаштування) → **"Settings for my calendars"**
3. Виберіть ваш основний календар
4. Перейдіть до розділу **"Share with specific people"** (Поділитися з конкретними людьми)
5. Натисніть **"Add people"** (Додати людей)
6. Введіть email Service Account (знайдіть його в Google Cloud Console → Service Accounts → ваш акаунт)
7. Виберіть рівень доступу: **"See all event details"** (Бачити всі деталі подій) або **"Make changes to events"** (Вносити зміни)
8. Натисніть **"Send"** (Надіслати)

### Варіант B: Створення окремого календаря для бота

1. У Google Calendar натисніть **"+"** біля "Other calendars"
2. Виберіть **"Create new calendar"** (Створити новий календар)
3. Введіть назву (наприклад: "Bot Bookings")
4. Створіть календар
5. Поділіться ним з Service Account (як у Варіанті A)

## Крок 6: Отримання Calendar ID

1. Відкрийте Google Calendar
2. У лівому меню знайдіть календар, який ви хочете використовувати
3. Натисніть на **"⋮"** (три крапки) біля назви календаря
4. Виберіть **"Settings and sharing"** (Налаштування та спільний доступ)
5. Прокрутіть вниз до розділу **"Integrate calendar"** (Інтеграція календаря)
6. Знайдіть **"Calendar ID"** (Ідентифікатор календаря)
   - Для основного календаря: зазвичай це ваш email
   - Для інших календарів: виглядає як `xxxxx@group.calendar.google.com`

## Крок 7: Налаштування .env файлу

1. Скопіюйте завантажений JSON файл у папку вашого проекту (наприклад: `service-account-key.json`)
2. Відкрийте файл `.env` у корені проекту
3. Додайте наступні рядки:

```env
# Google Calendar Service Account
GOOGLE_SERVICE_ACCOUNT_FILE=service-account-key.json
GOOGLE_CALENDAR_ID=your-email@gmail.com
```

**Приклад:**
```env
GOOGLE_SERVICE_ACCOUNT_FILE=service-account-key.json
GOOGLE_CALENDAR_ID=myemail@gmail.com
```

Або якщо використовуєте окремий календар:
```env
GOOGLE_SERVICE_ACCOUNT_FILE=service-account-key.json
GOOGLE_CALENDAR_ID=xxxxx@group.calendar.google.com
```

## Крок 8: Додавання файлу ключа до .gitignore

⚠️ **КРИТИЧНО ВАЖЛИВО**: Ніколи не комітьте файл з ключем у git!

1. Перевірте, що файл `service-account-key.json` (або як ви його назвали) додано до `.gitignore`
2. У файлі `.gitignore` має бути рядок:
   ```
   *.json
   ```
   або конкретно:
   ```
   service-account-key.json
   *-key.json
   ```

## Перевірка налаштування

Після налаштування перезапустіть бота та спробуйте забронювати прибирання. Бот має:

1. ✅ Показувати тільки вільні часові слоти з вашого календаря
2. ✅ Не показувати слоти, які вже зайняті
3. ✅ Не показувати вихідні дні (субота, неділя)
4. ✅ Показувати тільки робочий час (8:00-18:00)

## Усунення проблем

### Помилка: "Calendar not found" або "Permission denied"

**Рішення:**
- Перевірте, що Service Account має доступ до календаря (Крок 5)
- Перевірте правильність Calendar ID у `.env`
- Перевірте, що файл ключа знаходиться у правильній папці

### Помилка: "Invalid credentials"

**Рішення:**
- Перевірте шлях до файлу ключа у `.env`
- Перевірте, що файл JSON не пошкоджений
- Переконайтеся, що використовуєте правильний формат (JSON)

### Бот показує всі слоти (не перевіряє календар)

**Рішення:**
- Перевірте, що Google Calendar API увімкнено
- Перевірте логи бота на наявність помилок
- Перевірте, що Service Account має доступ до календаря

## Безкоштовність та обмеження

### ✅ Безкоштовна квота Google Calendar API

Google Calendar API надає **безкоштовну квоту** для всіх користувачів:

- **1,000,000 запитів на день** (1 мільйон)
- **250 запитів на 100 секунд на користувача**

### 📊 Скільки запитів використовує ваш бот?

Для вашого бота кожна перевірка доступних слотів = **1 запит до API**.

**Приклад використання:**
- Користувач обирає дату → 1 запит (перевірка зайнятих слотів)
- 100 користувачів на день → 100 запитів
- 1,000 користувачів на день → 1,000 запитів

**Висновок:** Навіть при 1,000 користувачів на день ви використаєте лише 0.1% від безкоштовної квоти!

### 💰 Коли може знадобитися платний план?

Платний план потрібен **тільки якщо**:
- Ви перевищите 1 мільйон запитів на день (дуже рідкісний випадок)
- Або потрібні додаткові функції (наприклад, запис подій у календар)

**Вартість:** $0.01 за 1,000 додаткових запитів (після безкоштовної квоти)

### 🎯 Для вашого бота

Для бота бронювання прибирання **безкоштовна квота більш ніж достатня**. Ви можете:
- Обслуговувати тисячі користувачів на день
- Перевіряти доступність слотів без обмежень
- Не турбуватися про витрати

### ⚠️ Важливо

1. **Безкоштовна квота діє завжди** - Google не стягує плату за перші 1 мільйон запитів на день
2. **Квота скидається щодня** - кожен новий день ви отримуєте нову квоту
3. **Service Account безкоштовний** - створення та використання Service Account не коштує нічого

## Додаткові ресурси

- [Google Calendar API Documentation](https://developers.google.com/calendar/api)
- [Google Calendar API Pricing](https://developers.google.com/calendar/api/guides/pricing)
- [Service Accounts Guide](https://cloud.google.com/iam/docs/service-accounts)
- [Google Cloud Console](https://console.cloud.google.com/)

---

**Готово!** Тепер ваш бот інтегрований з Google Calendar і буде показувати тільки вільні часові слоти. Інтеграція повністю безкоштовна для вашого випадку використання!

